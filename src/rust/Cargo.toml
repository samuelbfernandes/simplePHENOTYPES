[package]
name = 'simplephenotypes'
publish = false
version = '0.1.0'
edition = '2021'
rust-version = '1.65'

[lib]
crate-type = [ 'staticlib' ]
name = 'simplephenotypes'

[dependencies]
extendr-api = '*'

[profile.release]
lto = true
codegen-units = 1
