% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcriptome.R
\name{simulate_transcriptome}
\alias{simulate_transcriptome}
\title{Simulate a genetically controlled transcriptome}
\usage{
simulate_transcriptome(
  geno = NULL,
  n_genes = 1000,
  annotation = NULL,
  cis_window = 1e+06,
  h2 = "beta",
  cis_fraction = 0.25,
  n_factors = NULL,
  residual_module_fraction = 0.15,
  profile = "generic_bulk",
  seed = NULL,
  mimic = NULL,
  n_ind = NULL,
  epistasis = 0
)
}
\arguments{
\item{geno}{a \code{Population}, a Population-backed \code{phenotype_sim}, a
numeric-format genotype data frame, or an individuals-by-markers dosage matrix
coded -1/0/1. May be \code{NULL} for a \strong{purely non-genetic} transcriptome
(co-expression modules and gene noise only, every gene \code{h2 = 0}, no eQTL
tables); then \code{n_ind} sets the number of individuals and \code{h2}/\code{cis_fraction}/
\code{mimic} do not apply.}

\item{n_genes}{number of genes to simulate (ignored when \code{annotation} is given).}

\item{annotation}{optional gene annotation, a data frame with columns
\code{gene_id}, \code{chr}, \code{tss} (transcription start site, in the same physical units
as the genotype \code{pos}). When \code{NULL}, synthetic coordinates are generated.}

\item{cis_window}{cis window half-width in physical position units (default
\code{1e6}). A marker is cis to a gene if it is on the same chromosome within
\code{cis_window} of the TSS.}

\item{h2}{per-gene target expression heritability: \code{"beta"} (draw
\code{Beta(1.5, 6)}, mean 0.20), a single number, or a length-\code{n_genes} vector.}

\item{cis_fraction}{per-gene cis fraction of the marginal genetic variance
\code{omega}: \code{"beta"} (draw \code{Beta(2, 6)}, mean 0.25), a single number in \verb{[0, 1]},
or a length-\code{n_genes} vector.}

\item{n_factors}{number of latent regulatory factors \code{Q}; default
\code{min(50, max(5, ceiling(n_genes / 100)), n_ind - 2)}.}

\item{residual_module_fraction}{the residual module fraction \code{kappa}, a single
number in \verb{[0, 1]}: the \strong{target/expected} fraction of residual variance
carried by the shared module factor before normalization. As with \code{h2} and the
cis fraction, the realized fraction scatters around it in finite samples (the
scatter is large only at very small \code{n}).}

\item{profile}{a named calibration profile (currently \code{"generic_bulk"}).}

\item{seed}{optional seed (\code{NULL} or one non-negative whole number); the RNG
state is restored afterwards.}

\item{mimic}{optional \strong{user expression matrix} (genes x individuals, columns
named/ordered to the genotypes) to calibrate the generator to. When supplied,
the generator's \emph{targets} are set from it: exact per-gene mean and variance;
a per-gene heritability from a GREML estimator (REML on the genotypes' GRM,
single variance component); the co-expression factor count \code{n_factors}
(Marchenko-Pastur) and strength \code{kappa}. \code{n_genes} is taken from \code{mimic}. The
eQTL effects, loadings, and (downstream) phenotype slopes are still drawn de
novo -- mimic calibrates the \emph{distribution} of expression, it does not fit
individual effects. The estimates are returned in \verb{$calibration}. \code{kappa} is
a documented strength proxy, not an identifiable residual-module estimate.}

\item{n_ind}{number of individuals, \strong{required only when \code{geno = NULL}}
(otherwise taken from \code{geno}).}

\item{epistasis}{per-gene \strong{epistatic} fraction of the genetic variance
\code{epsilon}: \code{0} (default, purely additive cis/trans), a single number in
\verb{[0, 1]}, \code{"beta"} (draw \code{Beta(1.5, 6)}, mean 0.20), or a length-\code{n_genes}
vector. When positive, each gene gains 1--2 marker pairs whose centered dosage
\strong{product} (an additive-by-additive interaction) contributes to its expression; the
additive share is then \code{(1 - epsilon)} split by \code{cis_fraction}. Reported as an
\code{epi_eqtl} truth table and \code{v_epi} / \code{cis_epi_cov} / \code{trans_epi_cov} budget
rows; reconstructable and reusable by \code{\link[=predict.transcriptome_sim]{predict.transcriptome_sim()}}.}
}
\value{
a \code{transcriptome_sim}: \code{expression} and \code{genetic_expression}
(genes x individuals), \code{genes} (per-gene table: target and realized \code{h2},
target and realized cis fraction, module, coordinates, and \code{trans_scale}),
\code{cis_eqtl} (effective coefficient on centered dosage), \code{factor_eqtl} (raw
hub effects), and -- when \code{epistasis > 0} -- \code{epi_eqtl} (interacting marker
pairs: \code{snp1}, \code{snp2}, \code{effect}, \code{prod_mean}) truth tables, \code{loadings}, the
reference constants (marker means), the \code{profile}, \code{seed}, and a per-gene
variance budget. The budget closes as \code{v_cis + v_trans + v_epi + cis_trans_cov + cis_epi_cov + trans_epi_cov = Var(G)} (the epistatic terms
are zero without epistasis), plus \code{gr_cov}, the finite-sample
genetic-residual covariance \verb{2 Cov(G, R)}. The genetic component is
reconstructable from centered dosage: the cis part uses \code{cis_eqtl$effect}
directly; the trans part of a gene is \code{trans_scale} times its module's
\code{factor_eqtl$hub_effect} (loading 1); the epistatic part sums \code{epi_eqtl$effect}
times each pair's centered dosage product (\code{Z_snp1 * Z_snp2 - prod_mean}).
The truth tables are on the \strong{unit scale}; when \code{mimic} is used the returned
\code{genetic_expression} is affine-scaled to the mimicked moments, so this
reconstruction must be multiplied by the per-gene \code{reference$gene_scale}
(which is 1 without \code{mimic}) to match it. \code{mimic} also adds a \verb{$calibration}
element (the GREML \code{h2}, \code{mean}, \code{var} per gene, plus \code{n_factors} and \code{kappa}).
}
\description{
Generates a genes-by-individuals expression matrix as a hybrid latent-factor
eQTL model, fully parametrically -- no reference expression data or gene
annotation is required. For gene \eqn{g} the normalized expression of individual
\eqn{i} is \eqn{E_{gi} = \mu_g + G_{gi} + R_{gi}}, a genetic component
\eqn{G_{gi}} (a cis score over markers near the gene plus a trans score mediated
by latent regulatory factors, jointly scaled to a per-gene heritability
\eqn{h^2_g}) and a residual component \eqn{R_{gi}} (shared non-genetic
co-expression modules plus gene-specific noise). The cis fraction
\eqn{\omega_g}, residual module fraction \eqn{\kappa_g}, and \eqn{h^2_g} are
non-competing controls; because cis and trans covary (linkage disequilibrium,
structure), the combined genetic score is scaled jointly and the cis/trans
covariance is reported rather than assumed zero.
}
\details{
Per-marker reference means (for centering) and the drawn architecture (cis and
hub effects, loadings, module assignment) are computed on the supplied
(reference) individuals and returned, fixing the genetic scale in the spirit of
\code{additive_value()} / \code{phenotype_value()}; re-scoring the frozen architecture on
\emph{new} individuals (descendants, a cross, a selected subset) is supported by
\code{\link[=predict.transcriptome_sim]{predict.transcriptome_sim()}}. Defaults come from a named calibration
\code{profile}, a transparent benchmarking compromise, \strong{not} biological constants.

The genetic component \code{G} (scaled to \code{Var(G) = h2}) and the residual \code{R}
(scaled to \code{Var(R) = 1 - h2}, carrying the shared non-genetic module factor so
genes co-express through \code{kappa}) are drawn \strong{independently}, so \code{Cov(G, R) = 0}
in expectation. Realized heritability is reported as the finite-sample ratio
\code{Var(G) / Var(E)}; it tracks the target up to finite-sample \code{Cov(G, R)}, which
is itself reported (\code{var_budget$gr_cov}) rather than projected away -- the
package's realized-not-asserted variance convention. Degenerate genes realize
\code{h2 = 0}: a gene whose cis window holds no eligible marker \strong{and} whose module
factor is inert (which can happen on a small panel, where the union of the
module's genes' cis windows covers every marker, leaving no distant hub) has no
realizable genetic variance and gets \code{h2_realized = 0}.
}
\examples{
data("SNP55K_maize282_maf04")
tx <- simulate_transcriptome(SNP55K_maize282_maf04, n_genes = 200, seed = 1)
tx
dim(tx$expression)
}
\references{
Falconer DS, Mackay TFC (1996) \emph{Introduction to Quantitative Genetics},
4th ed. Longman, Harlow -- the additive dosage model and heritability used for
each gene's genetic component. The cis / trans / co-expression-module
architecture and its default calibration follow reported eQTL structure:
Albert et al. (2018) \emph{eLife}, \doi{10.7554/eLife.35471} (trans hotspots);
GTEx Consortium (2020) \emph{Science}, \doi{10.1126/science.aaz1776} (cis-eQTL
windows; multiple cis-eQTL per gene); Ouwens et al. (2020)
\emph{Eur. J. Hum. Genet.}, \doi{10.1038/s41431-019-0511-5} (cis/trans
expression heritability). Falconer & Mackay is cited only for the general
additive-dosage value and the definition of heritability; the cis / trans
latent-factor construction and its finite-sample standardization and scaling
are this package's own composite design, not attributed to a single source.
}
\seealso{
\code{\link[=simulate_phenotype]{simulate_phenotype()}}, \code{additive_value()}.
}
