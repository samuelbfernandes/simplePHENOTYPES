% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcriptome_layer.R
\name{transcriptome}
\alias{transcriptome}
\title{Transcriptome layer: a phenotype component driven by gene expression}
\usage{
transcriptome(sim, prop = NULL, n_genes = NULL, genes = NULL, slopes = NULL)
}
\arguments{
\item{sim}{a \code{phenotype_sim} carrying an expression source (see
\code{\link[=simulate_phenotype]{simulate_phenotype()}}'s \code{expression} / \code{transcriptome} arguments).}

\item{prop}{\strong{required} target share of phenotypic variance for the expression
component (single number, or one per trait). Unlike \code{\link[=additive]{additive()}}, it is \emph{not}
drawn from a remaining \code{h2} budget -- the expression component is a separate
variance category, bounded (with all layers) to sum to at most 1.}

\item{n_genes}{number of causal genes to draw (sparse); give this or \code{genes}.}

\item{genes}{an explicit causal-gene set (names matching the expression source's
row names, or integer gene indices); overrides \code{n_genes}.}

\item{slopes}{optional per-causal-gene slopes; a finite numeric vector, one per
causal gene. When \code{NULL}, slopes are drawn \code{N(0, 1)} (their absolute scale is
irrelevant -- the component is scaled to \code{prop}).}
}
\value{
the \code{phenotype_sim} with the transcriptome layer added and the
phenotype re-realized.
}
\description{
Adds a phenotype component that is a \strong{sparse linear function of
standardized gene expression} -- the continuous-predictor counterpart of
\code{\link[=additive]{additive()}}. It scores an expression source attached to the \code{phenotype_sim}
(a real matrix via \code{simulate_phenotype(expression =)}, or a genome-derived
transcriptome via \code{simulate_phenotype(transcriptome =)}): each causal gene's
expression is z-scored on the scored individuals, multiplied by a slope, and
summed; the sum is centered and scaled to the target variance share \code{prop},
exactly like \code{\link[=additive]{additive()}} (so only the relative slopes matter).
}
\details{
The expression-mediated component is a \strong{distinct variance category}, reported
on its own budget row and scaled by \code{prop} outside the marker \code{h2} budget --
because observed/derived expression is not purely additive-genetic. How much of
it counts as heritable depends on the source:
\itemize{
\item \strong{Derived} (\verb{transcriptome =}): the component splits into a
genetically-mediated part \code{Tx_g} (the share of expression traced to the
genome) and an environmentally-mediated part \code{Tx_e = Tx - Tx_g}. \code{Tx_g}
\strong{is} genetic, so it is added to \code{\link[=genetic_values]{genetic_values()}} and counts toward
realized H2; \code{\link[=mediation_split]{mediation_split()}} reports the realized
genetic / environmental / covariance shares of V_P.
\item \strong{Real} (\verb{expression =}): the genetic content of an observed matrix is
not asserted, so the whole component is treated as an environmental
predictor and \strong{excluded} from the genetic value and from H2.
}
A genotype-free basis (\code{simulate_phenotype(expression = ...)} with no \code{geno}),
\code{qtn_table()} gene rows, and cross-population reuse of an architecture
(\code{\link[=predict.transcriptome_sim]{predict.transcriptome_sim()}}) are all supported. Still-planned follow-ups:
\code{mimic} calibration from a user expression matrix, and an RNA-seq count
observation layer.
}
\examples{
data("SNP55K_maize282_maf04")
g <- SNP55K_maize282_maf04
tx <- simulate_transcriptome(g, n_genes = 200, seed = 1)
# phenotype driven by 20 causal genes of the derived transcriptome, plus a
# direct additive genome effect
ph <- simulate_phenotype(g, h2 = 0.5, seed = 2, transcriptome = tx) |>
  transcriptome(prop = 0.3, n_genes = 20) |>
  additive(prop = 0.2, n_qtn = 10)
ph
}
\seealso{
\code{\link[=simulate_phenotype]{simulate_phenotype()}}, \code{\link[=additive]{additive()}}, \code{\link[=mediation_split]{mediation_split()}},
\code{simulate_transcriptome()}.
}
