% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_population.R
\name{genotypic_value}
\alias{genotypic_value}
\title{Total genotypic value on a fixed, cross-generational scale}
\usage{
genotypic_value(x, qtn, a, d)
}
\arguments{
\item{x}{a \code{Population}, a Population-backed \code{phenotype_sim}, or a marker x
individual dosage matrix coded -1/0/1 (marker names as row names when \code{qtn} is
given by name; individual ids as column names).}

\item{qtn}{the causal loci, as marker names (matched against the map) or integer
marker (row) indices.}

\item{a}{a finite numeric vector of per-locus additive effects (departure of
the homozygote from the midpoint), one per entry of \code{qtn} and in the same
order (as in \code{\link[=additive_value]{additive_value()}}'s \code{effect}).}

\item{d}{a finite numeric vector of per-locus dominance deviations -- the value
of the heterozygote above the homozygous midpoint -- one per entry of \code{qtn}
and in the same order. Use \code{0} at a purely additive locus.}
}
\value{
a named numeric vector of total genotypic values, one per individual.
}
\description{
The additive-plus-dominance counterpart of \code{\link[=additive_value]{additive_value()}}. Scores each
individual by its \strong{total genotypic value}
\deqn{G_i = \sum_j \left[ a_j\,\mathrm{dosage}_{ij}
  + d_j\,\mathbf{1}(\mathrm{dosage}_{ij} = 0) \right]}
over a \strong{given, frozen architecture}: a per-locus additive effect \code{a} and
dominance deviation \code{d}, on the -1/0/1 dosage scale (so the heterozygote is
dosage 0), with \strong{no per-population centring or rescaling}. The per-locus term
is \code{-a / +d / +a} for gene content 0 / 1 / 2 -- exactly the raw genotypic value
an \code{additive(orthogonal = TRUE, a =, d =)} layer and a \code{dominance()} layer
build -- but held fixed so it is comparable across generations, like
\code{\link[=additive_value]{additive_value()}}.
}
\details{
This scores each individual's own \strong{per se} total genotypic value
\eqn{G = A + D}. It is \strong{not} a parental combining ability: a per se genotypic
value is not a parent's testcross merit -- at a pure-dominance locus (\code{a = 0},
\code{d > 0}) a heterozygous \code{Aa} parent scores above a homozygous \code{AA} parent, yet
crossed to an \code{aa} tester it is the \code{AA} parent whose progeny mean is higher, so
per se scores can reverse testcross ranking. A reciprocal-recurrent or hybrid
program therefore uses this by scoring the \strong{realized testcross / hybrid
progeny} (score the progeny population and dominance drives its mean), not the
parents per se.

It is also \strong{not} the transmissible breeding value. For selection on breeding
value use \code{\link[=select_ind]{select_ind()}} with \code{on = "bv"}, which scores the average-effect
breeding value \eqn{A_i = \sum_j \alpha_j (x_{ij} - 2 p_j)} with
\eqn{\alpha_j = a_j + d_j(1 - 2 p_j)}. Passing those \eqn{\alpha} to
\code{\link[=additive_value]{additive_value()}} yields only a \strong{ranking-equivalent} score
(\eqn{\alpha_j\,\mathrm{dosage}_{ij}}, which differs from \eqn{A_i} by an
additive constant), not the population-centred breeding value.
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:20)
# a = additive effects, d = dominance deviations (complete dominance at locus 5).
gv <- genotypic_value(pop, qtn = c(1, 5, 9),
                      a = c(0.5, -1, 2), d = c(0, 1, 0))
head(gv)
}
\references{
Falconer DS, Mackay TFC (1996) \emph{Introduction to Quantitative Genetics},
4th ed. Longman, Harlow -- the genotypic-value parameters \eqn{a} (departure
of the homozygote from the midpoint) and \eqn{d} (dominance deviation of the
heterozygote), and the average effect \eqn{\alpha = a + d(q - p)}.
}
\seealso{
\code{\link[=additive_value]{additive_value()}}, \code{\link[=phenotype_value]{phenotype_value()}}, \code{\link[=genetic_values]{genetic_values()}},
\code{\link[=select_ind]{select_ind()}}.
}
