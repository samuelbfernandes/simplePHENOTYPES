% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_population.R
\name{phenotype_value}
\alias{phenotype_value}
\title{Phenotype on a fixed, cross-generational scale}
\usage{
phenotype_value(
  x,
  qtn,
  effect,
  h2 = NULL,
  var_e = NULL,
  ref = NULL,
  seed = NULL
)
}
\arguments{
\item{x}{a \code{Population}, a Population-backed \code{phenotype_sim}, or a marker x
individual dosage matrix coded -1/0/1 (marker names as row names when \code{qtn} is
given by name; individual ids as column names).}

\item{qtn}{the causal loci, as marker names (matched against the map) or integer
marker (row) indices.}

\item{effect}{a finite numeric vector of per-locus additive effects, one per
entry of \code{qtn} and in the same order.}

\item{h2}{target narrow-sense heritability in \verb{(0, 1]}, used with \code{ref} to set a
fixed residual variance. Give exactly one of \code{h2} or \code{var_e}.}

\item{var_e}{fixed residual variance (a single non-negative number), on the same
scale as \code{Var(additive_value(x, qtn, effect))}. Give exactly one of \code{h2} or
\code{var_e}.}

\item{ref}{optional reference population/genotypes (same forms as \code{x}) whose
genetic variance converts \code{h2} to \code{var_e}; defaults to \code{x}. Ignored when
\code{var_e} is given.}

\item{seed}{optional seed for the residual draw -- \code{NULL} or one non-negative
whole number (the RNG state is restored afterwards), for reproducible
phenotypes.}
}
\value{
a named numeric vector of phenotypes, one per individual, with
attributes \code{var_e} (the fixed residual variance used) and \code{genetic_value} (the
fixed additive values).
}
\description{
The phenotypic counterpart of \code{\link[=additive_value]{additive_value()}}: each individual's phenotype is
its \strong{fixed} additive genetic value (frozen loci \code{qtn} and their \code{effect}s, on
the -1/0/1 dosage scale, with no per-population rescaling) plus an independent
residual \code{e ~ N(0, var_e)} on a \strong{fixed} residual-variance parameter \code{var_e}.
Because neither the genetic scale nor \code{var_e} is re-fit to the scored population,
the \strong{parametric (population) heritability} \code{Var(g) / (Var(g) + var_e)} \emph{declines}
as selection exhausts genetic variance -- which is exactly what a faithful
cross-generation \code{on = "pheno"} selection driver needs, and what
\code{\link[=genetic_values]{genetic_values()}} / \code{\link[=simulate_phenotype]{simulate_phenotype()}} cannot express (they re-scale the
genetic layer to its target \code{prop} on every population, so the genetic share, and
thus selection accuracy, never decays). (\code{var_e} is the residual \emph{variance
parameter}, not a sample-standardized value: \code{e} is a genuine normal draw, so its
realized sample variance scatters around \code{var_e} and \code{Cov(g, e) ~ 0} in
expectation. The package's sample statistic \code{Var(g)/Var(y)} therefore tracks the
parametric heritability up to finite-sample scatter, and is not forced to it.)
}
\details{
Supply \strong{exactly one} of \code{h2} or \code{var_e} to set that fixed residual variance:
\itemize{
\item \code{var_e} -- the residual variance directly. This is the robust choice for
multi-generation use: compute it once at the base generation and pass the
same value every cycle so it is truly frozen.
\item \code{h2} -- a target heritability, converted to a residual variance
\code{var_e = Var(g_ref) (1 - h2) / h2} from a reference population's genetic
variance. \code{ref} names that reference (default: \code{x} itself). For
cross-generation use pass the \strong{base} population as \code{ref} (or precompute
\code{var_e}); \code{h2} with the default \code{ref = x} re-derives \code{var_e} from each scored
population and so does \emph{not} freeze it across generations.
}
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:20)
# Freeze the residual variance from the base population at h2 = 0.5, then reuse
# it so later generations' heritability can decline as variance is exhausted.
y0 <- phenotype_value(pop, qtn = c(1, 5, 9), effect = c(0.5, -1, 2),
                      h2 = 0.5, seed = 1)
ve <- attr(y0, "var_e")
# a descendant population would then be scored with the same frozen var_e:
# phenotype_value(descendants, qtn = c(1, 5, 9), effect = c(0.5, -1, 2),
#                 var_e = ve, seed = 2)
head(y0)
}
\references{
Falconer DS, Mackay TFC (1996) \emph{Introduction to Quantitative Genetics},
4th ed. Longman, Harlow (heritability \eqn{h^2 = V_A / (V_A + V_E)}); Lynch M,
Walsh B (1998) \emph{Genetics and Analysis of Quantitative Traits}. Sinauer,
Sunderland, MA.
}
\seealso{
\code{\link[=additive_value]{additive_value()}}, \code{\link[=genetic_values]{genetic_values()}}, \code{\link[=select_ind]{select_ind()}},
\code{\link[=simulate_phenotype]{simulate_phenotype()}}.
}
