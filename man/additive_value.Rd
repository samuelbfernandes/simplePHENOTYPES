% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_population.R
\name{additive_value}
\alias{additive_value}
\title{Additive genetic value on a fixed, cross-generational scale}
\usage{
additive_value(x, qtn, effect)
}
\arguments{
\item{x}{a \code{Population}, a Population-backed \code{phenotype_sim}, or a marker x
individual dosage matrix coded -1/0/1 (marker names as row names when \code{qtn} is
given by name; individual ids as column names).}

\item{qtn}{the causal loci, as marker names (matched against the map) or integer
marker (row) indices.}

\item{effect}{a finite numeric vector of per-locus additive effects, one per
entry of \code{qtn} and in the same order.}
}
\value{
a named numeric vector of additive values, one per individual.
}
\description{
Scores each individual by \eqn{\sum_j \mathrm{dosage}_{ij}\,\mathrm{effect}_j}
over a \strong{given, frozen architecture} (loci \code{qtn} and their \code{effect}s), using the
-1/0/1 dosages directly with \strong{no per-population centring or rescaling}.
}
\details{
This is the accessor to use when comparing populations \emph{across generations} --
e.g. to show a selection response as the mean additive value climbs. It differs
from \code{\link[=genetic_values]{genetic_values()}}, which re-centres and re-scales every layer to its target
\code{prop} on whatever population is being scored: that makes \code{genetic_values()} read
roughly mean 0 / variance \code{prop} at \emph{every} generation, so it cannot express a
cross-generational trend. Here the loci and effects are fixed by the caller, so
the scale is stable and the numbers are comparable from one generation to the
next. Effects are on the same -1/0/1 dosage scale as an \code{additive()} layer's
\code{effect} (and as \code{\link[=qtn_table]{qtn_table()}}'s \code{effect} column).
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:20)
# Score on a fixed 3-locus architecture; the scale does not depend on the set.
av  <- additive_value(pop, qtn = c(1, 5, 9), effect = c(0.5, -1, 2))
head(av)
}
\seealso{
\code{\link[=genetic_values]{genetic_values()}}, \code{\link[=dosages]{dosages()}}, \code{\link[=qtn_table]{qtn_table()}}, \code{\link[=select_ind]{select_ind()}}.
}
