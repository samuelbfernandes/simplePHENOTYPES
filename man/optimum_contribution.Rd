% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ocs.R
\name{optimum_contribution}
\alias{optimum_contribution}
\title{Optimum contribution selection}
\usage{
optimum_contribution(
  x,
  merit = "bv",
  trait = 1L,
  direction = c("high", "low"),
  lambda = NULL,
  target_coancestry = NULL,
  max_coancestry = NULL,
  G = NULL,
  rep = 1L,
  min_contribution = 1e-04,
  max_iter = 10000L,
  tol = 1e-10
)
}
\arguments{
\item{x}{a \code{phenotype_sim} (supplies both merit and, via its genotypes, \code{G}) or
a \code{Population} (then \code{merit} must be a numeric vector or \code{G} is built from it
and \code{merit} supplied).}

\item{merit}{selection criterion: \code{"bv"} (default), \code{"gv"}, \code{"pheno"}, a
numeric vector (named by id or in order), or a function \code{merit(sim)} --- the
same hook as \code{\link[=select_ind]{select_ind()}}, so genomic/phenomic EBVs plug straight in.
Meuwissen's OCS maximizes the \emph{transmissible} merit of the parents' progeny,
so the default is the additive breeding value \code{"bv"} (the Fisher average-effect
projection), not the total genotypic value \code{"gv"}: with \code{"gv"} a
non-transmissible dominance/epistasis advantage (e.g. an overdominant
heterozygote) would be chased even though it is not passed on. \code{"bv"} is
unavailable for \code{architecture = "complex"}; pass numeric merit there.}

\item{trait}{trait index when \code{merit} is \code{"gv"}/\code{"pheno"} (default 1).}

\item{direction}{\code{"high"} (default) maximizes merit, \code{"low"} minimizes it.}

\item{lambda}{penalty on group coancestry (>= 0). Larger spreads contributions
and lowers coancestry. Give exactly one of \code{lambda}, \code{target_coancestry},
\code{max_coancestry}.}

\item{target_coancestry}{desired group coancestry \eqn{\tfrac12 c'Gc}; the
penalty is tuned to meet it (or the minimum attainable, with a warning).}

\item{max_coancestry}{as \code{target_coancestry}, but only enforced if the
unconstrained optimum exceeds it.}

\item{G}{optional precomputed relationship matrix (from \code{\link[=g_matrix]{g_matrix()}}); built
from \code{x} when omitted.}

\item{rep}{replication to read merit from (default 1).}

\item{min_contribution}{contributions below this are treated as zero when
listing selected parents (default 1e-4).}

\item{max_iter, tol}{Iteration cap (default 10000) and duality-gap tolerance
for the away-step Frank-Wolfe optimizer. A warning is issued if it does not
reach \code{tol} within \code{max_iter}, meaning the contributions may be sub-optimal.}
}
\value{
an \code{ocs} object: \code{contributions} (named, summing to 1), \code{parents} (ids
with contribution above \code{min_contribution}), \code{n_parents}, \code{merit} (expected
\eqn{c'g}), \code{coancestry} (\eqn{\tfrac12 c'Gc}), and \code{lambda}.
}
\description{
Chooses how much each candidate should contribute to the next generation to
maximize genetic merit while holding down relatedness --- the modern answer to
"truncation selection erodes diversity and long-term gain" (Meuwissen 1997). It
maximizes \eqn{c'g - \tfrac{\lambda}{2} c'Gc} over contributions \code{c} on the
simplex (\code{c >= 0}, \code{sum(c) = 1}), where \code{g} is merit and \code{G} the genomic
relationship matrix; the group coancestry of the chosen parents is
\eqn{\tfrac{1}{2}c'Gc}. Give a \code{lambda} to trace the gain--diversity frontier,
or a \code{target_coancestry} / \code{max_coancestry} and the penalty is found by
bisection.
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:40)
f1  <- cross(pop[1], pop[2], n = 1, seed = 1)
f2  <- selfcross(f1, n = 60, seed = 2)
ph  <- simulate_phenotype(f2, h2 = 0.5, seed = 3) |> additive(n_qtn = 30)

# Trace the frontier: more penalty -> lower coancestry, lower gain.
hi <- optimum_contribution(ph, merit = "bv", lambda = 1)
lo <- optimum_contribution(ph, merit = "bv", lambda = 50)
c(gain_hi = hi$merit, coan_hi = hi$coancestry,
  gain_lo = lo$merit, coan_lo = lo$coancestry)
}
\references{
Meuwissen THE (1997) Maximizing the response of selection with a
predefined rate of inbreeding. \emph{Journal of Animal Science}
75(4):934--940. \doi{10.2527/1997.754934x}
}
\seealso{
\code{\link[=g_matrix]{g_matrix()}}, \code{\link[=select_ind]{select_ind()}}, \code{\link[=recurrent_selection]{recurrent_selection()}}.
}
