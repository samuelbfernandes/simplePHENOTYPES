% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grammar_plot.R
\name{plot.phenotype_sim}
\alias{plot.phenotype_sim}
\title{Diagnostic plots for a simulated phenotype}
\usage{
\method{plot}{phenotype_sim}(x, which = c("variance", "hist", "effects", "cor"), ...)
}
\arguments{
\item{x}{a \code{phenotype_sim}.}

\item{which}{panels to draw; any of \code{"variance"}, \code{"hist"}, \code{"effects"},
\code{"cor"}. Defaults to all four in a 2x2 layout.}

\item{...}{named graphical parameters accepted by \code{\link[graphics:par]{graphics::par()}}.}
}
\value{
\code{x}, invisibly.
}
\description{
A quick visual summary of what a simulation produced, in base graphics (no
extra dependencies). Four panels:
\describe{
\item{\code{"variance"}}{the variance partition -- each layer's proportion plus
the residual -- as a stacked bar per trait.}
\item{\code{"hist"}}{the distribution of the realized phenotype (first trait).}
\item{\code{"effects"}}{the QTN effect sizes, from \code{\link[=qtn_table]{qtn_table()}}.}
\item{\code{"cor"}}{the genetic-value scatter between the first two traits (only
when \code{n_traits > 1}); otherwise realized vs target heritability.}
}
}
\examples{
data("SNP55K_maize282_maf04")
ph <- simulate_phenotype(SNP55K_maize282_maf04, n_traits = 2, h2 = 0.5,
                         seed = 1) |>
  additive(prop = 0.4, n_qtn = 8) |>
  dominance(prop = 0.1)
plot(ph)
}
\seealso{
\code{\link[=genetic_values]{genetic_values()}}, \code{\link[=qtn_table]{qtn_table()}}.
}
