% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_write.R
\name{mediation_split}
\alias{mediation_split}
\title{Genetic-mediated vs environmental split of a derived transcriptome component}
\usage{
mediation_split(sim)
}
\arguments{
\item{sim}{a \code{phenotype_sim}.}
}
\value{
A data frame (\code{trait}, \code{genetic_mediated}, \code{env_mediated},
\code{covariance}), or \code{NULL}.
}
\description{
For a phenotype built with a genome-\strong{derived} \code{transcriptome()} layer
(\code{simulate_phenotype(transcriptome = ...)}), the expression-mediated component
splits into a genetic-mediated part (the share of expression that traces to
the genome, which counts toward heritability and appears in
\code{\link[=genetic_values]{genetic_values()}}) and an environmental part. This returns that realized
split, per trait, as proportions of phenotypic variance:
\code{genetic_mediated}, \code{env_mediated}, and their \code{covariance} term
\code{2*Cov(Tx_g, Tx_e)/V_P} (finite-sample, ~0 because the generator draws the
genetic and non-genetic parts of expression independently). The three columns
sum to the realized expression-mediated share of variance.
}
\details{
Returns \code{NULL} when the phenotype has no derived transcriptome layer: a
\strong{real} expression source (\code{simulate_phenotype(expression = ...)}) has no
asserted genetic/environmental decomposition, so no split is reported.
}
\examples{
data("SNP55K_maize282_maf04")
tx <- simulate_transcriptome(SNP55K_maize282_maf04, n_genes = 100, seed = 1)
ph <- simulate_phenotype(SNP55K_maize282_maf04, h2 = 0.3, seed = 2,
                         transcriptome = tx) |>
  transcriptome(prop = 0.4, n_genes = 20)
mediation_split(ph)
}
\seealso{
\code{\link[=genetic_values]{genetic_values()}}, \code{\link[=transcriptome]{transcriptome()}}.
}
