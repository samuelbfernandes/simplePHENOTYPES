% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/usefulness.R
\name{cross_usefulness}
\alias{cross_usefulness}
\title{Predict the usefulness of candidate crosses}
\usage{
cross_usefulness(
  sim,
  pairs = NULL,
  scheme = c("dh", "selfcross", "cross"),
  n_progeny = 100L,
  generations = 5L,
  select_top = 0.1,
  trait = 1L,
  direction = c("high", "low"),
  seed = NULL
)
}
\arguments{
\item{sim}{a realized, Population-backed \code{phenotype_sim} whose candidate parents
are its individuals and whose additive layer supplies the scoring model.}

\item{pairs}{a two-column matrix or data frame of parent pairs (ids or
positions). \code{NULL} (default) uses all pairwise combinations of the parents ---
guard the count yourself for large panels.}

\item{scheme}{how the progeny family is derived: \code{"dh"} (doubled haploids, fully
inbred in one step), \code{"selfcross"} (self to near-inbred over \code{generations}), or
\code{"cross"} (single-cross progeny; only informative for non-inbred parents).}

\item{n_progeny}{family size simulated per cross.}

\item{generations}{selfing generations for \code{scheme = "selfcross"}.}

\item{select_top}{fraction of progeny whose intensity \code{i} sets the usefulness
horizon (default 0.1; smaller = a more elite target).}

\item{trait}{trait index to evaluate (default 1).}

\item{direction}{\code{"high"} (default) adds \code{i*sigma}; \code{"low"} subtracts it.}

\item{seed}{optional RNG seed for the whole evaluation.}
}
\value{
a data frame with \code{parent1}, \code{parent2}, \code{mean}, \code{sd}, \code{usefulness},
sorted best first, carrying attribute \code{intensity} (the \code{i} used).
}
\description{
For each candidate biparental cross, simulates a progeny family and reports its
additive genetic mean, genetic standard deviation, and \emph{usefulness}
\eqn{U = \mu + i\,\sigma} (Zhong \& Jannink 2007; Lehermeier et al. 2017). A
cross with a lower mean but more genetic variance can outrank a safer one,
which is the information parent-mean ranking misses.
}
\details{
\eqn{U = \mu + i\,\sigma} is a \strong{normal-theory criterion}: it equals the
expected genetic value of the best \code{select_top} fraction of progeny only when
that family's genetic values are approximately normally distributed, with \code{i}
the standardized selection intensity for the fraction. For a large
polygenic family this holds well; for a small family or one driven by a few
large-effect QTNs the distribution is discrete/skewed and \code{U} is an
approximation that can exceed the realized top-fraction mean (and even the
family maximum). Use it as a \emph{ranking} criterion under that assumption; read
the realized family from the returned \code{mean}/\code{sd} (or simulate the family
directly with \code{\link[=cross]{cross()}}/\code{\link[=selfcross]{selfcross()}}) if you need the exact tail mean.

Progeny are scored on the additive QTN effects already realized in \code{sim} (the
template), applied on a common scale, so the means and standard deviations are
directly comparable between crosses. Only the additive component enters the
score (the breeding-value basis of the usefulness criterion); dominance and
epistasis layers are ignored, and doubled-haploid / inbred families carry no
dominance anyway.
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:6)
sim <- simulate_phenotype(pop, h2 = 0.5, seed = 1) |> additive(n_qtn = 30)
u <- cross_usefulness(sim, scheme = "dh", n_progeny = 30, seed = 2)
head(u)   # crosses ranked by expected best-progeny value
}
\references{
Zhong S, Jannink J-L (2007) Using quantitative trait loci results to
discriminate among crosses on the basis of their progeny mean and variance.
\emph{Genetics} 177(1):567--576. \doi{10.1534/genetics.107.075358};
Lehermeier C, Teyssedre S, Schon C-C (2017) Genetic gain increases by applying
the usefulness criterion with improved variance prediction in selection of
crosses. \emph{Genetics} 207(4):1651--1661. \doi{10.1534/genetics.117.300403}
}
\seealso{
\code{\link[=cross]{cross()}}, \code{\link[=single_seed_descent]{single_seed_descent()}}, \code{\link[=select_ind]{select_ind()}}.
}
