% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_write.R
\name{qtn_table}
\alias{qtn_table}
\title{The QTNs behind a simulation, with their effects}
\usage{
qtn_table(sim, rep = 1L)
}
\arguments{
\item{sim}{a \code{phenotype_sim}.}

\item{rep}{replication whose QTN architecture to report (default 1). This
matters when the simulation used \code{vary_qtn = TRUE}.}
}
\value{
A data frame with columns \code{trait}, \code{layer}, \code{set}, \code{snp}, \code{chr},
\code{pos}, \code{maf}, \code{effect}, \code{var_explained}, \code{QTN_t1}, \code{QTN_t2} and \code{ld_r2}, or
a zero-row frame when no layers have been added.
}
\description{
One row per QTN per trait per layer, naming the marker and the effect it was
given. This is the v2 equivalent of the v1 \code{Additive_QTNs.txt} /
\code{QTN_effects_summary.txt} files, assembled from the simulation object rather
than written to disk.
}
\details{
Epistatic QTNs come in interacting sets, so each member of a set gets its own
row, sharing the set's \code{effect} and identified by a common \code{set} number.
For every other layer type \code{set} is \code{NA}.

Under \code{architecture = "ld"} the two traits have distinct causal loci that are
in linkage disequilibrium. \code{QTN_t1} and \code{QTN_t2} name the linked pair's causal
SNP for trait 1 and trait 2 (shown on both of the pair's rows so each row is
self-contained), and \code{ld_r2} is the squared correlation between them. All
three are \code{NA} for the other architectures. (For \code{ld_type = "indirect"} the
hidden cause-of-LD locus is not shown here -- it is not a QTN -- but is
available programmatically on the layer.)
}
\examples{
data("SNP55K_maize282_maf04")
ph <- simulate_phenotype(SNP55K_maize282_maf04, h2 = 0.5, seed = 1) |>
  additive(prop = 0.3, n_qtn = 3) |>
  epistasis(prop = 0.2, n_pairs = 2)

qtn_table(ph)

# Which markers were used, and how large were the effects?
subset(qtn_table(ph), layer == "additive")
}
\seealso{
\code{\link[=genetic_values]{genetic_values()}}.
}
