% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transcriptome_counts.R
\name{observe_counts}
\alias{observe_counts}
\title{Draw RNA-seq counts from a simulated transcriptome}
\usage{
observe_counts(
  tx,
  library_size = 1,
  baseline = 3,
  coupling = 0.5,
  dispersion = 0.1,
  seed = NULL
)
}
\arguments{
\item{tx}{a \code{transcriptome_sim} from \code{\link[=simulate_transcriptome]{simulate_transcriptome()}}.}

\item{library_size}{per-individual depth factor \code{L_i}: one positive number, or
one per individual. Default \code{1} (no depth variation).}

\item{baseline}{per-gene log baseline count \code{alpha_g}: one number or one per
gene. Default \code{3} (a mean of about \code{exp(3)} counts at \code{z = 0}).}

\item{coupling}{latent-to-log-mean slope \code{sigma_g}: one non-negative number or
one per gene. Default \code{0.5}.}

\item{dispersion}{NB dispersion \code{phi_g} (\verb{Var = mu + phi mu^2}): one
non-negative number or one per gene. \code{0} gives a Poisson limit. Default \code{0.1}.}

\item{seed}{optional seed; the RNG state is restored afterwards.}
}
\value{
the \code{transcriptome_sim} with a \code{counts} matrix (genes x individuals,
integer) and a \code{count_model} list (the per-gene/per-individual parameters and
the realized mean matrix \code{mu}).
}
\description{
Maps the normalized latent expression of a \code{transcriptome_sim} to integer
counts through a negative-binomial observation model. For gene \code{g} and
individual \code{i}, with \verb{z_\{gi\}} the latent expression standardized per gene,
\deqn{\log \mu_{gi} = \log L_i + \alpha_g + \sigma_g z_{gi}, \qquad
      Y_{gi} \sim \mathrm{NB}(\mu_{gi}, \phi_g),}
where \code{L_i} is a per-individual library-size (sequencing-depth) factor,
\code{alpha_g} a per-gene log baseline count, \code{sigma_g} the latent-to-log-mean
coupling, and \code{phi_g} the NB dispersion (\verb{Var = mu + phi mu^2}). Standardizing
the latent expression makes \code{sigma_g} a per-standard-deviation log-fold change,
comparable across genes and unaffected by any \code{mimic} rescaling.
}
\details{
\strong{Count-scale heritability differs from the latent heritability.} The link is
nonlinear and mean-dependent, so the fraction of count variance explained by the
genome is not the latent \code{h2}; treat counts as an observation layer for
count-based methods, not as a re-parameterization of the genetic model.
}
\examples{
data("SNP55K_maize282_maf04")
tx <- simulate_transcriptome(SNP55K_maize282_maf04, n_genes = 50, seed = 1)
tx <- observe_counts(tx, seed = 2)
tx$counts[1:5, 1:5]
}
\seealso{
\code{\link[=simulate_transcriptome]{simulate_transcriptome()}}.
}
