% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemes.R
\name{bulk}
\alias{bulk}
\title{Bulk advance}
\usage{
bulk(x, generations = 5L, n = NULL, seed = NULL)
}
\arguments{
\item{x}{a \code{Population}, or a \code{phenotype_sim} built on one (its genotypes are
used).}

\item{generations}{number of selfing generations.}

\item{n}{bulk size carried to the next generation (default: keep the current
size).}

\item{seed}{optional RNG seed for the whole scheme.}
}
\value{
a \code{Population} (the final bulk).
}
\description{
Advances a population as an undivided bulk: each generation every plant is
selfed and the progeny are pooled, then \code{n} are carried forward at random
(Bernardo 2020). Line identity is not tracked.
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:10)
f1  <- cross(pop[1], pop[2], n = 30, seed = 1)
bk  <- bulk(f1, generations = 4, n = 30, seed = 2)
n_individuals(bk)
}
\references{
Bernardo R (2020) \emph{Breeding for Quantitative Traits in
Plants}, 3rd ed. Stemma Press, Woodbury, Minnesota.
}
\seealso{
\code{\link[=single_seed_descent]{single_seed_descent()}}, \code{\link[=pedigree]{pedigree()}}, \code{\link[=recurrent_selection]{recurrent_selection()}}.
}
