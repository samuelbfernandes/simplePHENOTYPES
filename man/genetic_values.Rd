% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io_write.R
\name{genetic_values}
\alias{genetic_values}
\title{Genetic values behind the simulated phenotypes}
\usage{
genetic_values(sim, rep = 1L)
}
\arguments{
\item{sim}{a \code{phenotype_sim}.}

\item{rep}{replication to retrieve (default 1).}
}
\value{
An individuals-by-traits numeric matrix, with individual IDs as row
names and trait names as column names.
}
\description{
The genetic component of each individual's phenotype: the sum of the
scaled additive, dominance and epistatic layers, before the residual is
added. This is the quantity the v1 engine wrote to \code{Genetic_values.txt}.
}
\details{
Variance QTL are deliberately excluded: a \code{vqtl()} layer modulates the
spread of the residual rather than contributing a genetic value, so it has
no place in this matrix. By default this returns replication 1. When the
simulation used \code{vary_qtn = TRUE}, use \code{rep} to retrieve the architecture
and genetic values belonging to another replication.
}
\examples{
data("SNP55K_maize282_maf04")
ph <- simulate_phenotype(SNP55K_maize282_maf04, n_traits = 2, h2 = 0.5,
                         seed = 1) |>
  additive(n_qtn = 5)

g <- genetic_values(ph)
head(g)

# Realized genetic correlation between the two traits
round(cor(g[, 1], g[, 2]), 3)

# Realized heritability: genetic variance over phenotypic variance
pheno <- phenotypes_wide(ph)
round(var(g[, 1]) / var(pheno$Trait_1), 3)
}
\seealso{
\code{\link[=qtn_table]{qtn_table()}} for the loci and effects that produce these values,
\code{\link[=phenotypes_long]{phenotypes_long()}} for the phenotypes themselves.
}
