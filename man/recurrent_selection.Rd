% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/schemes.R
\name{recurrent_selection}
\alias{recurrent_selection}
\title{Recurrent selection}
\usage{
recurrent_selection(
  x,
  phenotype,
  cycles = 3L,
  n_parents = 10L,
  n_crosses = NULL,
  progeny_per_cross = 10L,
  on = "pheno",
  trait = 1L,
  direction = "high",
  seed = NULL
)
}
\arguments{
\item{x}{a \code{Population}, or a \code{phenotype_sim} built on one (its genotypes are
used).}

\item{phenotype}{a function mapping a \code{Population} to a realized
\code{phenotype_sim} (e.g. \code{function(p) simulate_phenotype(p, ...) |> additive(...)}). Called once per generation to score the current
population. Fix the causal loci with \code{additive(qtn = ...)} if the same QTNs
should act every generation.}

\item{cycles}{number of selection cycles.}

\item{n_parents}{number of parents selected each cycle.}

\item{n_crosses}{number of crosses among the selected parents (default:
\code{n_parents}, a random-mating chain).}

\item{progeny_per_cross}{progeny produced per cross.}

\item{on, trait, direction}{passed to \code{\link[=select_ind]{select_ind()}}.}

\item{seed}{optional RNG seed for the whole scheme.}
}
\value{
a \code{Population} (the final cycle), carrying attribute \code{history}.
}
\description{
Cycles of select-then-intermate for population improvement (Bernardo 2020;
Falconer \& Mackay 1996): each cycle the population is phenotyped, the best parents
are selected, and they are intercrossed to form the next cycle's population.
}
\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:30)
pheno <- function(p) {
  simulate_phenotype(p, h2 = 0.5, seed = 7) |> additive(n_qtn = 50)
}
out <- recurrent_selection(pop, pheno, cycles = 3, n_parents = 10,
                           progeny_per_cross = 10, seed = 2)
attr(out, "history")
}
\references{
Bernardo R (2020) \emph{Breeding for Quantitative Traits in
Plants}, 3rd ed. Stemma Press, Woodbury, Minnesota; Falconer DS, Mackay TFC
(1996) \emph{Introduction to Quantitative Genetics}, 4th ed. Longman, Harlow.
}
\seealso{
\code{\link[=select_ind]{select_ind()}}, \code{\link[=pedigree]{pedigree()}}.
}
