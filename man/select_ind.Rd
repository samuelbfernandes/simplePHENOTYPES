% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/select.R
\name{select_ind}
\alias{select_ind}
\title{Select individuals from a simulated population}
\usage{
select_ind(
  sim,
  n = NULL,
  prop = NULL,
  intensity = NULL,
  on = "pheno",
  trait = 1L,
  direction = c("high", "low"),
  method = c("mass", "within_family", "among_family", "combined", "index",
    "quadratic_index", "random"),
  family = NULL,
  weights = NULL,
  quad_weights = NULL,
  h2 = NULL,
  family_relationship = 0.25,
  rep = 1L
)
}
\arguments{
\item{sim}{a realized \code{phenotype_sim}, ideally built on a \code{Population} so the
selected individuals can be crossed on.}

\item{n}{number of individuals to keep. Exactly one of \code{n}, \code{prop}, \code{intensity}.}

\item{prop}{proportion of individuals to keep (0-1).}

\item{intensity}{standardized selection intensity \code{i}; the number kept is the
count whose expected intensity under normality is closest to \code{i}.}

\item{on}{selection criterion (see the Criterion section).}

\item{trait}{trait index to select on (default 1); ignored for \code{method = "index"}, which uses all traits.}

\item{direction}{\code{"high"} (default) keeps the largest scores, \code{"low"} the
smallest.}

\item{method}{selection method (see the Method section).}

\item{family}{optional grouping vector (length = individuals) for the family
methods.}

\item{weights}{economic weights (one per trait) for \code{method = "index"}, or the
linear weights \code{w} for \code{method = "quadratic_index"}.}

\item{quad_weights}{symmetric \verb{n_traits x n_traits} matrix of quadratic
(diagonal) and cross-product (off-diagonal) weights \code{W} for
\code{method = "quadratic_index"} (default: zero, i.e. a linear index).}

\item{h2}{narrow-sense heritability of the selection trait, required by
\code{method = "combined"} to weight family versus individual information.}

\item{family_relationship}{additive relationship among family members for
\code{method = "combined"} (default 0.25 = half-sibs; use 0.5 for full-sibs or
selfed families).}

\item{rep}{replication to select on when several were simulated (default 1).}
}
\value{
the selected individuals as a \code{Population} (when \code{sim} is
Population-backed) or their ids, carrying attributes \code{selected} (ids),
\code{differential} (selection differential S on the criterion), \code{intensity}
(realized standardized i), \code{criterion} and \code{method}.
}
\description{
Ranks the individuals of a realized \code{\link[=simulate_phenotype]{simulate_phenotype()}} result on a chosen
criterion and returns the selected ones as a \link[=as_population]{Population},
ready to cross forward. This is the truncation-selection primitive the
breeding-scheme wrappers (\code{\link[=single_seed_descent]{single_seed_descent()}}, \code{\link[=bulk]{bulk()}}, \code{\link[=pedigree]{pedigree()}},
\code{\link[=recurrent_selection]{recurrent_selection()}}) build on.
}
\section{Criterion (\code{on})}{

\describe{
\item{\code{"pheno"}}{the observed phenotype -- realistic mass selection. For a
purely additive model this is the classic \eqn{R = i\,h^2\,\sigma_P}
(narrow-sense \eqn{h^2}; Falconer & Mackay). When dominance/epistasis layers
are present the simulated components are not Fisher-orthogonal (SPEC S2), so
only the additive fraction is transmitted and the realized breeding-value
response is governed by the additive variance, not by broad-sense \eqn{H^2}.}
\item{\code{"gv"}}{the true \emph{total} genetic value (broad-sense: additive plus any
dominance/epistasis). Idealized selection on genetic merit -- an upper bound
on selectable genetic value, but not on breeding-value response, since the
non-additive part is not transmitted to progeny.}
\item{\code{"bv"}}{the true \emph{breeding} value -- the classical transmissible merit,
\eqn{A_i = \sum_j \alpha_j (x_{ij} - 2p_j)}, summing each causal locus's
average effect of substitution \eqn{\alpha_j = a_j + d_j(q_j - p_j)}
(\code{\link[=.breeding_value_matrix]{.breeding_value_matrix()}}). The per-locus effects are reconstructed from
the simulation's own additive/dominance QTN effects (it is a simulation, so
they are known exactly), making this the genetic value transmitted to
random-mated progeny -- robust to both linkage disequilibrium (exact for an
F2) and departures from HWE (after inbreeding/selection). It captures the
additive average effects dominance loci induce away from \eqn{p = 0.5} and
reduces to the additive value for a purely additive model. This is the merit
that governs response to selection in the next generation. Not available
when the model has an epistasis layer or \code{architecture = "complex"}: an
epistatic term has no per-locus \eqn{a}/\eqn{d}, so its induced additive
average effects cannot be reconstructed and the breeding value would be
incomplete (see \code{\link[=.breeding_value_matrix]{.breeding_value_matrix()}}); both cases error rather than
return a partial value. Supply your own predicted values via a
numeric/function criterion there.}
\item{a numeric vector}{one score per individual (named by id or in
population order) -- the hook for \strong{genomic selection, phenomic selection}
and any predicted/estimated value you compute externally.}
\item{a function}{\code{on(sim)} returning such a vector -- the same hook, resolved
at selection time.}
}
\code{on} applies to \code{method = "mass"}, \code{"within_family"}, \code{"among_family"} and
\code{"combined"}. The multi-trait index methods (\code{"index"}, \code{"quadratic_index"})
score on all traits' true breeding values and ignore \code{on} (a single
per-individual score cannot supply per-trait breeding values); supplying one
with an index method warns. To rank on externally predicted values, compute
your index and pass it through \code{on} with \code{method = "mass"}.
}

\section{Method}{

\code{"mass"} truncates on the individual criterion. \code{"within_family"} keeps the top
fraction inside each family, \code{"among_family"} keeps whole top-ranked families,
and \code{"combined"} ranks on the Lush combined index that optimally weights an
individual's own record and its family mean to predict breeding value; the
weights are the selection-index solution \eqn{b = V^{-1} c} built from \code{h2} and
the within-family additive relationship \code{family_relationship} (0.25 half-sibs,
0.5 full-sibs), following Falconer \& Mackay (1996) and Lynch \& Walsh (1998).
\code{"index"} is
the Smith--Hazel multi-trait economic index (\code{weights} = economic weights, one
per trait): \eqn{b = P^{-1} G a}, selecting on \eqn{b'y}. \code{"quadratic_index"} is
the nonlinear genomic selection index of Ceron-Rojas et al. (2026),
\eqn{\hat I = w'y + y'Wy} (\code{weights} = linear \code{w}, \code{quad_weights} = the
symmetric quadratic/cross-product matrix \code{W}), which captures trait interactions
and intermediate optima. \code{"random"} draws at random (a drift control). Family
methods need a \code{family} grouping; \code{"combined"} additionally needs \code{h2}.
}

\examples{
data("SNP55K_maize282_maf04")
pop <- as_population(SNP55K_maize282_maf04, individuals = 1:60)
f1  <- cross(pop[1], pop[2], n = 1, seed = 1)
f2  <- selfcross(f1, n = 50, seed = 2)
ph  <- simulate_phenotype(f2, h2 = 0.5, seed = 3) |> additive(n_qtn = 20)

top <- select_ind(ph, prop = 0.2, on = "pheno")   # keep the best 20\%
n_individuals(top)
}
\references{
Truncation response and selection intensity: Falconer DS, Mackay TFC (1996)
Introduction to Quantitative Genetics, 4th ed. Longman, Harlow; Lynch M,
Walsh B (1998) Genetics and Analysis of Quantitative Traits. Sinauer
Associates, Sunderland, Massachusetts.
Combined (family + individual) selection: Lush JL (1947) Family merit and
individual merit as bases for selection. \emph{The American Naturalist}
81:241--261 (Part I) and 362--379 (Part II). \doi{10.1086/281520}
Multi-trait selection index \eqn{b = P^{-1} G a}: Smith HF (1936) A discriminant
function for plant selection. \emph{Annals of Eugenics} 7(3):240--250.
\doi{10.1111/j.1469-1809.1936.tb02143.x}; Hazel LN (1943) The genetic basis
for constructing selection indexes. \emph{Genetics} 28(6):476--490.
\doi{10.1093/genetics/28.6.476}
Quadratic (nonlinear) genomic selection index: Ceron-Rojas JJ,
Montesinos-Lopez OA, Montesinos-Lopez A, et al. (2026) Nonlinear genomic
selection index accelerates multi-trait crop improvement. \emph{Nature
Communications} 17:1991. \doi{10.1038/s41467-026-69890-3}
Additive average-effect (breeding value) decomposition used as the merit for
the \code{"index"} / \code{"quadratic_index"} methods (per-locus average effect of
substitution \eqn{\alpha = a + d(q - p)}): Fisher RA (1918) The correlation between
relatives on the supposition of Mendelian inheritance. \emph{Transactions of
the Royal Society of Edinburgh} 52:399--433. \doi{10.1017/S0080456800012163};
Falconer DS, Mackay TFC (1996) Introduction to Quantitative Genetics, 4th
ed. Longman; Lynch M, Walsh B (1998) Genetics and Analysis of
Quantitative Traits. Sinauer.
Breeding schemes: Bernardo R (2020) Breeding for Quantitative Traits in Plants,
3rd ed. Stemma Press.
}
\seealso{
\code{\link[=single_seed_descent]{single_seed_descent()}}, \code{\link[=bulk]{bulk()}}, \code{\link[=pedigree]{pedigree()}},
\code{\link[=recurrent_selection]{recurrent_selection()}}, \code{\link[=simulate_phenotype]{simulate_phenotype()}}.
}
